/*package Chapter6;

import java.util.Scanner;

public class FindErrors {
    public static void main(String[] args) {
        for (int i = 10; i > 0; i++) {// the update should be i--
            System.out.print(i);
            // x=y;//declare and initialize x and y
            // a=b;//declare and initialize a and b
        }

        // 1-b
        int sum = 0;
        int num = 0;// declared and initialize num
        Scanner scanner = new Scanner(System.in);
        do {
            num = scanner.nextInt();
            sum += num;
            //num++;
        }
        /*until*/ /*while (sum > 10000);// not used until just to used while
        //1-c
        while (x < 1 && x > 10) {// it is not true statment it is always false
            a = b;
        }
        //1-d
        while (a == b) ;  // it is infinitive loop so we  stop the loop using break
        {
            a == b;
            x = y;
        }
    }
}*/